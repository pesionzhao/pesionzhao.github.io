整理自己参与过的知识要点

1. 什么是堆，什么是栈
2. 二级指针代表什么, 以及llm.c中的三级指针
3. microGrad中adam优化器的原理
4. 交叉熵,BCE损失函数
5. softmax, sigmiod作用和原理
6. 工程经验-- 训练时的一些技巧
7. 讲述排序法，哈希原理
8. 转置卷积是什么？
   

CUDA

1. 矩阵乘法为什么要Tile

对于`n*n`的矩阵来说，输出矩阵的一个点需要`2*n`个输入数据计算得到, 而每个输出点要被使用n次, 这个重复的过程如果每次都是从显存中读取,会非常低效

- v1: native
- v2: shared memory存一些Tile
- v3: 一个线程计算若干个点
- v4: 优化load进shared memory的过程， 重排至合并？
- v5: float4类型，连续访存4个浮点数
- v6: 减小bank冲突
- v7: 降低shared memory的读取
- v8：流水并行，计算和读取同时进行
- v9: float4循环展开

1. 提高GPU要注意哪些?

避免分支发散, 减小数据移动的次数

3. 计时方法, 先预热,在重复求平均

